git commit -a -m “Primera pregunta”

Enviar a heroku
git push heroku master
Enviar a github
git push origin master
heroku open


Versiones
git log --oneline

añadir a la última versión
git commit --amend -m “Primera pregunta”

ver cambios
git status